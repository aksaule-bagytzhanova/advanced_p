#Modes
#r - opens a file for reading.
#w - opens a file for writing
#x - opens a file for exclusive creation. If the file already
#exists, the operation fails
#a - opens a file appending at the end 

#Open file
f = open("test.txt")
